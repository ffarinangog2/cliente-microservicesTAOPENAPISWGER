
server:
  port: 8081
  forward-headers-strategy: framework

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/clientes_db
    username: postgres
    password: postgres


  jpa:
    hibernate:
      ddl-auto: update


